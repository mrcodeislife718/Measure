const form=document.querySelector('#contact-form');const out=document.querySelector('#contact-result');form?.addEventListener('submit',async(e)=>{e.preventDefault();out.textContent='Sending…';try{const body=Object.fromEntries(new FormData(form));const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.message||d.error||`HTTP ${r.status}`);out.textContent=d.message;form.reset();}catch(error){out.textContent=error instanceof Error?error.message:String(error);}});