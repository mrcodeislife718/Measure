{
  "name": "measure-eval-os",
  "version": "0.1.0",
  "private": false,
  "description": "Architecture-neutral evaluation operating system for measuring intelligent systems, benchmarks, verifiers, and deployment trust.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:runtime": "find api public scripts -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "check": "npm run check:runtime && npm run build && npm test",
    "test": "node --test dist/test/**/*.test.js",
    "demo": "npm run build && node dist/src/demo.js",
    "private-runner": "npm run build && node dist/src/private-runner.js",
    "dev": "npx vercel dev",
    "deploy:preview": "npx vercel",
    "deploy:prod": "npx vercel --prod",
    "smoke:prod": "node scripts/smoke-production.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
